name: ya_ok
description: Я ОК — швидкий зв'язок з близькими. Один дотик — спокій для родини.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase - відключено (працюємо в офлайн-режимі)
  # firebase_core: ^2.24.2
  # firebase_auth: ^4.15.3
  # cloud_firestore: ^4.13.6
  # firebase_messaging: ^14.7.9
  
  # State Management
  provider: ^6.1.1
  
  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.2
  
  # Биометрія
  local_auth: ^2.1.8
  
  # UI/UX
  flutter_local_notifications: ^17.0.0
  
  # Utils
  intl: ^0.18.1
  connectivity_plus: ^5.0.2
  http: ^1.1.0
  url_launcher: ^6.2.0
  
  # Icons
  cupertino_icons: ^1.0.6

  # MeshGram
  uuid: ^4.5.1
  # flutter_nearby_connections: ^1.1.2  # Тимчасово вимкнено через проблеми з AGP 8.x

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.7

flutter:
  uses-material-design: true
  
  # Assets будуть додані після організації файлів
  # assets:
  #   - assets/
