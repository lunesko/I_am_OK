# ⚡ Швидкий старт з організацією файлів

## 🎯 3 кроки

---

## 📥 Крок 1: Дочекатися завантаження

**УВАГА:** Всі файли з розширенням `.crdownload` ще завантажуються!

✅ **Перевір:**
- Відкрий папку `logo/`
- Переконайся, що всі файли завантажені (без `.crdownload`)

---

## 🚀 Крок 2: Запустити скрипт

### **У PowerShell:**

```powershell
# Перейти в папку logo
cd "M:\I am OK\logo"

# Запустити скрипт
.\organize-files.ps1
```

### **Що робить скрипт:**
1. ✅ Перейменовує файли (видаляє `.crdownload`)
2. ✅ Створює структуру папок `assets/`
3. ✅ Переміщує файли у правильні папки

---

## ✅ Крок 3: Перевірити

### **Після запуску скрипта:**

```
assets/
├── logo/
│   ├── app-icon/ (13 файлів)
│   ├── notification/ (4 файли)
│   └── favicon/ (2 файли)
├── icons/
│   ├── status/ (16 файлів)
│   ├── navigation/ (20 файлів)
│   └── action/ (16 файлів)
└── ui-elements/
    ├── status-cards/ (4 файли)
    ├── buttons/ (4 файли)
    ├── cards/ (3 файли)
    └── inputs/ (2 файли)
```

---

## 📝 Додати у Flutter

### **pubspec.yaml вже оновлено!**

Просто запусти:
```bash
flutter pub get
```

---

## 🎨 Використання в коді

```dart
// Логотип
Image.asset('assets/logo/app-icon/logo-appIcon-512.png')

// Іконка
Image.asset('assets/icons/status/ok/icon-status-ok-64.png')

// UI Елемент
Image.asset('assets/ui-elements/status-cards/ui-status-ok.png')
```

---

## ❓ Проблеми?

### **Файли ще завантажуються:**
- Дочекайся завершення
- Перевір, що немає `.crdownload`

### **Скрипт не працює:**
- Перевір права виконання PowerShell
- Запусти від імені адміністратора

### **Файли не переміщені:**
- Перевір, чи створені папки `assets/`
- Перевір шляхи у скрипті

---

**Готово!** Запусти `organize-files.ps1` та все буде організовано автоматично. 🚀
