# 🧪 Інструкція з тестування "Я ОК"

## 📱 Тестування на емуляторі

### Android Emulator

1. **Створити емулятор:**
   ```bash
   # Відкрити Android Studio
   # Tools → Device Manager → Create Device
   # Оберіть Pixel 5 або інший пристрій
   # API Level: 33+ (Android 13+)
   ```

2. **Запустити:**
   ```bash
   flutter emulators --launch <emulator_id>
   flutter run
   ```

### iOS Simulator

1. **Відкрити Simulator:**
   ```bash
   open -a Simulator
   ```

2. **Запустити:**
   ```bash
   flutter run -d ios
   ```

---

## ✅ Чек-лист тестування

### 1. Автентифікація

- [ ] **Вхід через Дія ID:**
  - Ввести email та ім'я
  - Перевірити створення профілю в Firestore
  - Перевірити збереження токену

- [ ] **Вхід через BankID:**
  - Ввести номер телефону та ім'я
  - Перевірити створення профілю

- [ ] **Біометрія:**
  - Face ID / Touch ID працює
  - Пропуск біометрії працює
  - Помилка біометрії обробляється

- [ ] **Вихід:**
  - Користувач виходить з акаунту
  - Дані очищаються локально

### 2. Головний екран

- [ ] **Відправка чекіну:**
  - Натискання кнопки "Відправити"
  - Вибір статусу (ОК, Зайнятий, Пізніше, Обійми)
  - Анімація підтвердження
  - Збереження в Firestore

- [ ] **Оффлайн-режим:**
  - Вимкнути інтернет
  - Натиснути "Відправити"
  - Перевірити збереження в Hive
  - Увімкнути інтернет
  - Перевірити автоматичну синхронізацію

- [ ] **Останній чекін:**
  - Відображається час останнього чекіну
  - Формат дати правильний

### 3. Екран "Мої люди"

- [ ] **Список контактів:**
  - Відображаються додані контакти
  - Статус прочитання показується
  - Аватари відображаються

- [ ] **Додавання контакту:**
  - Пошук користувача
  - Додавання до списку
  - Оновлення в Firestore

- [ ] **Зворотній зв'язок:**
  - Кнопка "Вдома все добре" працює
  - Відправка повідомлення

### 4. Push-сповіщення

- [ ] **Отримання сповіщення:**
  - Створити чекін
  - Перевірити отримання push на іншому пристрої
  - Перевірити вміст сповіщення

- [ ] **Натискання на сповіщення:**
  - Відкривається додаток
  - Навігація до потрібного екрану

- [ ] **Foreground сповіщення:**
  - Локальне сповіщення показується
  - Звук та вібрація працюють

### 5. Налаштування

- [ ] **Нагадування:**
  - Вибір часу нагадування
  - Збереження налаштувань
  - Сповіщення приходить вчасно

- [ ] **Попередження близьких:**
  - Вибір періоду (2, 3, 5, 7 днів)
  - Збереження налаштувань

- [ ] **Тихий режим:**
  - Увімкнення/вимкнення
  - Сповіщення не звучать вночі

- [ ] **Оффлайн-режим:**
  - Увімкнення/вимкнення
  - Автосинхронізація працює

- [ ] **Правові документи:**
  - Посилання на Privacy Policy працює
  - Посилання на Terms працює
  - Посилання на Support працює

### 6. Безпека

- [ ] **Геолокація:**
  - Додаток НЕ запитує доступ до геолокації
  - Перевірити в налаштуваннях пристрою

- [ ] **Шифрування:**
  - Дані зберігаються зашифрованими
  - Біометрія захищає доступ

- [ ] **Firestore Rules:**
  - Користувач не може читати чужі дані
  - Користувач не може змінювати чужі чекіни

---

## 🐛 Тестування помилок

### Помилки мережі

1. **Відсутність інтернету:**
   - Вимкнути Wi-Fi та мобільні дані
   - Спробувати відправити чекін
   - Перевірити збереження оффлайн
   - Увімкнути інтернет
   - Перевірити синхронізацію

2. **Повільне з'єднання:**
   - Використати Network Throttling в DevTools
   - Перевірити таймаути
   - Перевірити показ індикатора завантаження

### Помилки Firebase

1. **Невдала автентифікація:**
   - Неправильний email
   - Перевірити показ помилки
   - Перевірити можливість повторної спроби

2. **Невдале збереження:**
   - Симулювати помилку Firestore
   - Перевірити збереження оффлайн
   - Перевірити повторну спробу

### Помилки пристрою

1. **Відсутність біометрії:**
   - Емулятор без Face ID
   - Перевірити fallback на PIN

2. **Немає дозволу на сповіщення:**
   - Відмовити в дозволі
   - Перевірити показ інструкцій
   - Перевірити можливість повторного запиту

---

## 📊 Тестування продуктивності

### Час відгуку

- [ ] Відкриття додатку: < 2 секунди
- [ ] Відправка чекіну: < 1 секунда
- [ ] Завантаження списку контактів: < 1 секунда
- [ ] Синхронізація оффлайн: < 5 секунд

### Використання пам'яті

```bash
# Android
adb shell dumpsys meminfo app.poruch.yaok

# iOS
Instruments → Allocations
```

### Використання батареї

- [ ] Додаток не споживає батарею в фоні
- [ ] Оффлайн-режим економить батарею

---

## 🔍 Тестування на реальних пристроях

### Android

1. **Підключити пристрій:**
   ```bash
   adb devices
   flutter run
   ```

2. **Перевірити:**
   - Push-сповіщення на реальному пристрої
   - Біометрія (якщо є)
   - Різні розміри екранів

### iOS

1. **Підключити пристрій:**
   - Підключити через USB
   - Довірити комп'ютеру
   - `flutter run -d ios`

2. **Перевірити:**
   - Face ID / Touch ID
   - Push-сповіщення
   - Різні розміри екранів

---

## 🧪 Unit тести

```bash
flutter test
```

**Приклад тесту:**

```dart
// test/services/auth_service_test.dart
import 'package:flutter_test/flutter_test.dart';
import 'package:ya_ok/services/auth_service.dart';

void main() {
  group('AuthService', () {
    test('signInWithDiaID creates user if not exists', () async {
      final service = AuthService();
      final result = await service.signInWithDiaID(
        'test@example.com',
        'Test User',
      );
      
      expect(result, isNotNull);
      expect(result?.user?.email, 'test@example.com');
    });
  });
}
```

---

## 🔄 Integration тести

```bash
flutter drive --target=test_driver/app.dart
```

**Приклад:**

```dart
// test_driver/app.dart
import 'package:flutter_driver/flutter_driver.dart';
import 'package:test/test.dart';

void main() {
  group('Я ОК App', () {
    FlutterDriver driver;

    setUpAll(() async {
      driver = await FlutterDriver.connect();
    });

    tearDownAll(() async {
      await driver.close();
    });

    test('checkin flow', () async {
      // Знайти кнопку "Відправити"
      final sendButton = find.byValueKey('send_button');
      await driver.tap(sendButton);
      
      // Перевірити анімацію
      final successMessage = find.byValueKey('success_message');
      await driver.waitFor(successMessage);
    });
  });
}
```

---

## 📝 Чек-лист перед релізом

- [ ] Всі тести пройдені
- [ ] Немає критичних помилок
- [ ] Продуктивність прийнятна
- [ ] Безпека перевірена
- [ ] Оффлайн-режим працює
- [ ] Push-сповіщення працюють
- [ ] Біометрія працює
- [ ] UI/UX перевірено
- [ ] Різні розміри екранів перевірені
- [ ] Різні версії ОС перевірені

---

## 🆘 Troubleshooting тестів

### Проблема: Тести не запускаються

```bash
flutter clean
flutter pub get
flutter test
```

### Проблема: Емулятор не запускається

```bash
# Android
emulator -avd <avd_name>

# iOS
open -a Simulator
```

### Проблема: Firebase не підключається в тестах

Використайте моки або Firebase Emulator:

```bash
firebase emulators:start
```

---

**Готово! 🎉** Тепер ваш додаток протестований та готовий до релізу.
