# 🚀 Розгортання Firestore Rules та Indexes

## ✅ Що вже зроблено:

1. ✅ Створено `firebase.json` - конфігурація Firebase
2. ✅ Створено `.firebaserc` - активний проект `i-am-ok-2f7b9`
3. ✅ Розгорнуто `firestore.rules` - правила безпеки
4. ✅ Розгорнуто `firestore.indexes.json` - індекси для оптимізації

---

## 📋 Команди для розгортання:

### **Розгорнути тільки правила:**
```bash
firebase deploy --only firestore:rules
```

### **Розгорнути тільки індекси:**
```bash
firebase deploy --only firestore:indexes
```

### **Розгорнути все:**
```bash
firebase deploy --only firestore
```

### **Dry run (перевірка без розгортання):**
```bash
firebase deploy --only firestore:rules --dry-run
```

---

## 🔍 Перевірка статусу:

### **Переглянути активний проект:**
```bash
firebase use
```

### **Змінити проект:**
```bash
firebase use i-am-ok-2f7b9
```

### **Переглянути правила в консолі:**
https://console.firebase.google.com/project/i-am-ok-2f7b9/firestore/rules

---

## 📊 Структура файлів:

```
.
├── firebase.json              # Конфігурація Firebase
├── .firebaserc                # Активний проект
├── firestore.rules            # Правила безпеки
└── firestore.indexes.json    # Індекси для запитів
```

---

## ⚠️ Важливо:

1. **Індекси:** Після розгортання індекси можуть створюватися до 24 годин
2. **Правила:** Застосовуються миттєво після розгортання
3. **Тестування:** Завжди тестуйте правила перед production

---

## 🧪 Тестування правил:

### **Варіант 1: Firebase Console**
1. Відкрити Firebase Console → Firestore → Rules
2. Натиснути "Rules Playground"
3. Тестувати різні сценарії

### **Варіант 2: Firebase Emulator**
```bash
firebase emulators:start --only firestore
```

---

**Зроблено в Україні 🇺🇦**
