# Сценарій двостороннього тестування Ya OK
**Дата:** 3 лютого 2026
**Пристрої:**
- 🔷 **Device 1:** SM-A525F (фізичний) - `RZ8T11LV55F`
- 🔶 **Device 2:** Medium Phone AVD (емулятор) - `emulator-5554`

---

## ✅ Підготовка завершена

### Статус встановлення:
- ✅ APK зібрано: BUILD SUCCESSFUL
- ✅ Встановлено на SM-A525F
- ✅ Встановлено на емулятор
- ✅ Обидва додатки запущені

---

## 🧪 Тестові сценарії

### СЦЕНАРІЙ 1: Генерація та обмін QR кодами

#### На Device 1 (SM-A525F):
1. Відкрити Family → натиснути іконку QR
2. QR код згенеровано з:
   - Device 1 ID
   - Ім'я користувача
   - X25519 публічний ключ
3. Зробити скріншот QR коду

#### На Device 2 (Emulator):
1. Відкрити Family → натиснути іконку сканера
2. Вручну ввести дані з QR Device 1:
   - ID: `<з Device 1>`
   - Ім'я: `Device 1 User`
3. Натиснути "Додати"

#### Команди моніторингу:
```powershell
# Device 1 логи
adb -s RZ8T11LV55F logcat | Select-String "QR|x25519|Identity"

# Device 2 логи
adb -s emulator-5554 logcat | Select-String "contact_add_request|Adding new contact"
```

#### Очікуваний результат:
- ✅ Device 2: контакт "Device 1 User" додано
- ✅ Device 2 відправляє `contact_add_request` до Device 1
- ✅ Device 1: контакт автоматично з'являється (bidirectional add)

**Логи Device 2:**
```
🔵 Sending contact_add_request: id=<D2_id>, name=Device2, to=<D1_id>
🔵 SendTextTo result: 0 (to <D1_id>)
```

**Логи Device 1:**
```
🔵 Found contact_add_request, processing...
✅ Adding new contact: Device2
```

---

### СЦЕНАРІЙ 2: Відправка повідомлень через Bluetooth

#### Підготовка:
1. Увімкнути Bluetooth на обох пристроях
2. Перевірити індикатори: 📶 має бути зелений

#### На Device 1:
1. Main → вибрати статус "💚 Я ОК"
2. Натиснути "Надіслати"
3. Вибрати контакт "Device 2 User"
4. Натиснути "Надіслати обраним"

#### Команди моніторингу:
```powershell
# Device 1 - відправка
adb -s RZ8T11LV55F logcat | Select-String "📤 create_and_send|SendTextTo|Known peers"

# Device 2 - прийом
adb -s emulator-5554 logcat | Select-String "📥 handle_incoming|Packet deserialized|Message decrypted"
```

#### Очікувані логи Device 1:
```
📤 create_and_send_packet_to: recipient=<D2_id>
📤 Known peers count: 1
📤 Found peer: <D2_id> at Ble
📤 Peer has x25519 key, length=32
✅ Created encrypted packet, sending...
✅ Packet sent successfully
```

#### Очікувані логи Device 2:
```
📥 handle_incoming_packet_internal: bytes=256, peer_info=Some((Ble, "XX:XX:XX:XX"))
📥 Packet deserialized: sender=<D1_id>
📥 Updating peer: <D1_id> at XX:XX:XX:XX via Ble
✅ Peer updated in router
✅ Message decrypted successfully
📥 Processing decrypted message
```

#### Перевірка результату:
1. Device 2 → Inbox → вкладка "📥 Вхідні"
2. Має з'явитись повідомлення: "💚 Я ОК" від Device 1

---

### СЦЕНАРІЙ 3: Відправка через Internet (Relay)

#### Підготовка:
1. Вимкнути Bluetooth на обох
2. Підключити обидва до WiFi/Internet
3. Перевірити індикатори: 🌐 має бути зелений

#### На Device 2:
1. Main → вибрати "💛 Все нормально, зайнятий"
2. Натиснути "Надіслати"
3. Вибрати "Device 1 User"
4. Надіслати

#### Команди моніторингу:
```powershell
# Обидва пристрої
adb -s emulator-5554 logcat | Select-String "Internet|Relay|send_to"
adb -s RZ8T11LV55F logcat | Select-String "Internet|Relay|handle_incoming"
```

#### Очікуваний результат:
- Device 2 відправляє через relay сервер
- Device 1 отримує через relay
- Перевірити Inbox → Вхідні: "💛 Все нормально, зайнятий"

---

### СЦЕНАРІЙ 4: Вибір кількох контактів

#### На Device 1:
1. Додати ще один контакт вручну:
   - ID: `test_contact_3`
   - Ім'я: `Test User 3`
2. Main → вибрати "💙 Зателефоную пізніше"
3. Натиснути "Надіслати"
4. В діалозі вибрати:
   - ☑️ Device 2 User
   - ☑️ Test User 3
5. Надіслати обраним

#### Очікувані логи:
```
🔵 Sending to selected contacts: [<D2_id>, test_contact_3]
📤 create_and_send_packet_to: recipient=<D2_id>
📤 create_and_send_packet_to: recipient=test_contact_3
```

#### Перевірка:
- Device 2 має отримати повідомлення
- Test User 3 не отримає (не існує)

---

### СЦЕНАРІЙ 5: Вкладки Inbox

#### На Device 2:
1. Inbox → вкладка "📬 Всі"
   - Показує всі повідомлення
2. Вкладка "📥 Вхідні"
   - Тільки від Device 1
3. Вкладка "📤 Вихідні"
   - Тільки від Device 2 (власні)

#### Команди перевірки:
```powershell
adb -s emulator-5554 logcat | Select-String "TabType|renderMessages|INCOMING|OUTGOING"
```

#### Очікуваний результат:
- Правильна фільтрація по sender_id
- Іконки напрямку (↓ для вхідних, ↑ для вихідних)

---

### СЦЕНАРІЙ 6: Індикатори з'єднань

#### На обох пристроях перевірити footer:

**З увімкненим всім:**
```
📶 Bluetooth (зелений)  🔗 Mesh (зелений)  🌐 Internet (зелений)
```

**Вимкнути Bluetooth:**
```powershell
adb -s RZ8T11LV55F shell svc bluetooth disable
```
Результат:
```
📵 Bluetooth (сірий)  🔗 Mesh (зелений)  🌐 Internet (зелений)
```

**Вимкнути WiFi:**
```powershell
adb -s RZ8T11LV55F shell svc wifi disable
```
Результат:
```
📵 Bluetooth (сірий)  ⛓️‍💥 Mesh (сірий)  🚫 Internet (сірий)
```

---

## 📊 Автоматичні команди тестування

### Повний цикл моніторингу:
```powershell
# Одночасний моніторинг обох пристроїв
Start-Job -Name "Device1" -ScriptBlock {
    adb -s RZ8T11LV55F logcat | Select-String "📤|📥|🔵|✅|❌"
}

Start-Job -Name "Device2" -ScriptBlock {
    adb -s emulator-5554 logcat | Select-String "📤|📥|🔵|✅|❌"
}

# Переглянути логи
Receive-Job -Name "Device1" -Keep | Select-Object -Last 20
Receive-Job -Name "Device2" -Keep | Select-Object -Last 20
```

### Очистити логи та почати знову:
```powershell
adb -s RZ8T11LV55F logcat -c
adb -s emulator-5554 logcat -c
```

### Перезапустити додатки:
```powershell
adb -s RZ8T11LV55F shell am force-stop app.poruch.ya_ok
adb -s emulator-5554 shell am force-stop app.poruch.ya_ok
Start-Sleep -Seconds 2
adb -s RZ8T11LV55F shell am start -n app.poruch.ya_ok/.MainActivity
adb -s emulator-5554 shell am start -n app.poruch.ya_ok/.MainActivity
```

---

## ✅ Чеклист тестування

### Базові функції:
- [ ] Запуск на обох пристроях
- [ ] UI відображається коректно
- [ ] Індикатори з'єднань працюють
- [ ] Navigation між екранами

### Обмін контактами:
- [ ] Генерація QR з x25519 на Device 1
- [ ] Сканування/введення на Device 2
- [ ] Автоматичне додавання на Device 1 (bidirectional)
- [ ] Контакти відображаються в обох

### Доставка повідомлень:
- [ ] Device 1 → Device 2 через Bluetooth
- [ ] Device 2 → Device 1 через Internet
- [ ] Вибір одного контакту
- [ ] Вибір декількох контактів
- [ ] "Надіслати всім"

### Inbox функції:
- [ ] Вкладка "Всі" показує всі
- [ ] Вкладка "Вхідні" фільтрує правильно
- [ ] Вкладка "Вихідні" фільтрує правильно
- [ ] Іконки напрямку відображаються

### Транспорти:
- [ ] Bluetooth (BLE) доставка
- [ ] WiFi Direct (mesh) доставка
- [ ] Internet (relay) доставка
- [ ] Автоматичне перемикання

### Шифрування:
- [ ] X25519 обмін ключами
- [ ] E2E шифрування повідомлень
- [ ] Розшифровка на отримувачі
- [ ] Помилки при неправильних ключах

---

## 🎯 Критерії успіху

### Must have (критичні):
1. ✅ Bidirectional contact addition працює
2. ✅ Повідомлення доставляються через будь-який транспорт
3. ✅ E2E шифрування працює
4. ✅ Inbox фільтрація працює
5. ✅ Вибір контактів працює

### Should have (важливі):
1. ✅ Індикатори показують реальний стан
2. ✅ Логи детальні для діагностики
3. ✅ UI responsive та стабільний
4. ✅ Немає крашів

### Nice to have (бажані):
1. Швидкість доставки < 3 секунди
2. Offline queue працює
3. Retry механізм для failed messages

---

## 📝 Формат звіту

Після кожного сценарію записати:

```
✅/❌ СЦЕНАРІЙ N: [Назва]
Пройдено: [так/ні]
Час виконання: [X секунд]
Проблеми: [опис або "немає"]
Логи: [attach або посилання]
```

---

## 🚀 Початок тестування

**Команда для старту:**
```powershell
Write-Host "=== ПОЧАТОК ДВОСТОРОННЬОГО ТЕСТУВАННЯ ===" -ForegroundColor Green
Write-Host "Device 1: SM-A525F (RZ8T11LV55F)" -ForegroundColor Cyan
Write-Host "Device 2: Emulator (emulator-5554)" -ForegroundColor Cyan
Write-Host "`nСтатус: Готово до тестування" -ForegroundColor Yellow
```

**Готові до виконання сценаріїв 1-6!**
