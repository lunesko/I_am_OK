# ЕДИНЫЙ КОНЦЕПТ «Я ОК»
## (Core / Mesh / Tactical / Civil / Orbit — это РЕЖИМЫ, не версии)

## 1. Главное правило архитектуры

> **Приложение одно.  
> Ядро одно.  
> Режим определяется СРЕДОЙ, а не сборкой.**

Никаких:

* разных APK
* разных App Store версий
* разных кодовых баз

## 2. Как это работает логически

### Приложение всегда запускается в режиме:

```
ADAPTIVE MODE
```

Ядро само понимает:

* есть ли интернет
* есть ли BLE
* есть ли Wi-Fi Direct
* есть ли спутниковый канал
* есть ли ограничения (война / госсреда)

## 3. Таблица: режимы как поведение, не как продукт

| Условие среды       | Поведение ядра        |
| ------------------- | --------------------- |
| Есть интернет       | Использует IP + P2P   |
| Нет интернета       | BLE / Wi-Fi mesh      |
| Плохая связь        | Store & Forward       |
| Массовая нагрузка   | Приоритет Status      |
| Жёсткие ограничения | Минимум трафика       |
| Коллапс             | Только локальный mesh |

**Никаких переключателей “военный режим”.**  
Это опасно и глупо.

## 4. Единая модель функционала

### Что ВСЕГДА доступно

* Сигнал «Я ОК»
* Короткий текст
* Короткое аудио
* Темный режим
* Шифрование
* Локальное хранение
* Автопередача при появлении канала

## 5. Как ядро выбирает транспорт

### Алгоритм выбора (упрощённо)

```
if internet_available:
    use IP + P2P
else if wifi_direct_available:
    use Wi-Fi Direct
else if ble_available:
    use BLE
else:
    store_and_wait
```

Параллельно может использовать **несколько каналов**.

## 6. Приоритеты всегда одни и те же

| Тип    | Приоритет |
| ------ | --------- |
| Status | MAX       |
| Text   | MED       |
| Voice  | LOW       |

Это не меняется ни при каких режимах.

## 7. Пакет — универсальный

Один формат пакета летит:

* через интернет
* через mesh
* через спутник
* через человека с телефоном в кармане

📌 **Среда не знает содержимого.  
Содержимое не знает среды.**

## 8. Обновлённая структура Rust-core (без режимов)

```
ya_ok_core/
├── core/          # идентичность, крипта, сообщения
├── transport/     # каналы (появились — используем)
├── routing/       # всегда один DTN
├── storage/       # всегда локально
├── sync/          # всегда gossip
├── policy/        # ограничения среды
├── api/           # FFI
```

## 9. Новый модуль: policy/

### Зачем он нужен

**Policy ≠ режим**

Policy — это **ограничения среды**, которые ядро учитывает.

```rust
struct Policy {
    max_packet_size,
    max_voice_len,
    max_storage,
    allowed_transports,
}
```

### Примеры

* гражданка → всё доступно
* война → запрет гео, приоритет статусов
* коллапс → только BLE, минимум данных

Policy может:

* определяться автоматически
* приходить обновлением
* быть зашитой по умолчанию

## 10. UI тоже один

### Пользователь видит:

* одну кнопку
* один интерфейс
* одно поведение

Он **не знает**, в каком режиме работает система.  
И не должен.

## 11. Почему это стратегически правильно

* меньше багов
* меньше веток кода
* меньше атак
* проще сертификация
* легче масштабирование
* легче open-source

## 12. Очень важный момент (из практики)

> **Любая кнопка “Военный режим”  
> рано или поздно станет уликой.**

Адаптивная система — нет.

## 13. Итог одной строкой

> **«Я ОК» — это не приложение с режимами.  
> Это единый протокол человеческого присутствия.**
