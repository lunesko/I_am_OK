# 🎯 Збірка через Android Studio

## 📋 Покрокова інструкція:

### **Крок 1: Відкрити проект**

1. Запустити **Android Studio**
2. **File** → **Open**
3. Вибрати папку `M:\I am OK\android`
4. Натиснути **OK**

### **Крок 2: Дозволити синхронізацію**

1. Android Studio запропонує синхронізувати Gradle
2. Натиснути **"Sync Now"** або **"Trust Project"**
3. Дочекатися завершення синхронізації (індикатор внизу)

### **Крок 3: Перевірити налаштування**

1. **File** → **Project Structure** (Ctrl+Alt+Shift+S)
2. Перевірити:
   - **SDK Location** → вказано правильний Android SDK
   - **Gradle Version** → 8.5
   - **Android Gradle Plugin** → автоматично

### **Крок 4: Зібрати APK**

#### Варіант A: Debug APK
1. **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
2. Дочекатися завершення збірки
3. Натиснути **"locate"** у спливаючому повідомленні
4. APK буде в: `app\build\outputs\apk\debug\app-debug.apk`

#### Варіант B: Через меню
1. **Build** → **Make Project** (Ctrl+F9)
2. Потім **Build** → **Build APK(s)**

### **Крок 5: Встановити на пристрій**

1. Підключити Android пристрій через USB
2. Увімкнути **USB Debugging** на пристрої
3. В Android Studio: **Run** → **Run 'app'** (Shift+F10)
4. Або перетягнути APK на пристрій

---

## ⚙️ Налаштування для успішної збірки:

### **1. Перевірити Java**

1. **File** → **Settings** (Ctrl+Alt+S)
2. **Build, Execution, Deployment** → **Build Tools** → **Gradle**
3. **Gradle JDK** → вибрати:
   - `jbr-21` (Java з Android Studio)
   - або `C:\Program Files\Android\Android Studio2\jbr`

### **2. Очистити кеш (якщо потрібно)**

1. **File** → **Invalidate Caches / Restart**
2. Вибрати **"Invalidate and Restart"**
3. Дочекатися перезапуску

### **3. Синхронізувати Gradle**

1. **File** → **Sync Project with Gradle Files**
2. Або кнопка 🐘 (Gradle Sync) у панелі інструментів

---

## 🔧 Вирішення проблем:

### **Проблема: "Gradle sync failed"**

**Рішення:**
1. **File** → **Settings** → **Build Tools** → **Gradle**
2. Змінити **Gradle JDK** на `jbr-21`
3. **File** → **Sync Project with Gradle Files**

### **Проблема: "SDK not found"**

**Рішення:**
1. **File** → **Project Structure** → **SDK Location**
2. Вказати шлях до Android SDK:
   - `C:\Users\marty\AppData\Local\Android\sdk`

### **Проблема: "Build failed"**

**Рішення:**
1. **Build** → **Clean Project**
2. **Build** → **Rebuild Project**
3. Перевірити помилки в **Build** → **Build Output**

---

## 📱 Альтернатива: Запуск на емуляторі

### **1. Створити емулятор:**

1. **Tools** → **Device Manager**
2. **Create Device**
3. Вибрати пристрій (наприклад, Pixel 5)
4. Вибрати системний образ (API 33+)
5. **Finish**

### **2. Запустити:**

1. Запустити емулятор з Device Manager
2. **Run** → **Run 'app'** (Shift+F10)
3. Або натиснути ▶️ (Run) у панелі інструментів

---

## 📊 Переваги Android Studio:

✅ **Автоматичне налаштування** Java/Gradle  
✅ **Візуальний інтерфейс** для дебагу  
✅ **Вбудований емулятор**  
✅ **Логування** в реальному часі  
✅ **Профілювання** продуктивності  
✅ **Автодоповнення** коду  

---

## 🎯 Швидкий старт:

1. Відкрити `android/` в Android Studio
2. Дочекатися синхронізації
3. **Build** → **Build APK(s)**
4. Готово! 🎉

---

**Після успішної збірки APK буде в:**
```
M:\I am OK\android\app\build\outputs\apk\debug\app-debug.apk
```
