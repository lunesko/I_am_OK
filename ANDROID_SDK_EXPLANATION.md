# 📱 Пояснення Android SDK налаштувань

## 🔍 Що таке `compileSdk`?

`compileSdk = 35` означає, що ваш додаток компілюється **проти Android SDK версії 35** (Android 15).

---

## 📊 Різниця між SDK налаштуваннями:

### **1. `compileSdk` (Compile SDK)**
```gradle
compileSdk = 35
```
- **Що це:** Версія Android SDK, проти якої компілюється код
- **Навіщо:** Дозволяє використовувати нові API та функції з цієї версії
- **Важливо:** Не впливає на мінімальну версію Android для запуску додатку
- **Приклад:** Якщо `compileSdk = 35`, ви можете використовувати API з Android 15

### **2. `minSdk` (Minimum SDK)**
```gradle
minSdk = flutter.minSdkVersion  // Зазвичай 21 (Android 5.0)
```
- **Що це:** Мінімальна версія Android, на якій може запускатися додаток
- **Навіщо:** Визначає найстаріший Android, який підтримує ваш додаток
- **Приклад:** Якщо `minSdk = 21`, додаток не запуститься на Android 4.4

### **3. `targetSdk` (Target SDK)**
```gradle
targetSdk = flutter.targetSdkVersion  // Зазвичай найновіша версія
```
- **Що це:** Версія Android, для якої оптимізовано додаток
- **Навіщо:** Вказує Android, що ваш додаток протестовано та оптимізовано
- **Важливо:** Впливає на поведінку системи (дозволи, безпека тощо)

---

## 🎯 Чому важливо правильно налаштувати:

### **compileSdk = 35:**
✅ **Переваги:**
- Доступ до новітніх API Android 15
- Плагіни можуть використовувати нові функції
- Краща підтримка сучасних пристроїв

❌ **Недоліки:**
- Може мати проблеми зі стабільністю (як у вашому випадку)
- Потребує новіші версії Android Studio та Gradle
- Може виникати конфлікт з Java Linker (jlink.exe)

### **compileSdk = 34:**
✅ **Переваги:**
- Більш стабільний
- Краща сумісність з інструментами
- Менше проблем з компіляцією

❌ **Недоліки:**
- Деякі плагіни можуть вимагати SDK 35
- Немає доступу до найновіших API

---

## 🔧 Поточна ситуація в проекті:

```gradle
compileSdk = 34  // Використовується зараз
minSdk = flutter.minSdkVersion  // Зазвичай 21
targetSdk = flutter.targetSdkVersion  // Зазвичай 34
```

### **Чому знижено до 34:**
1. SDK 35 має проблеми з Java Linker (jlink.exe)
2. Більшість плагінів працюють з SDK 34
3. Краща стабільність збірки

### **Якщо потрібен SDK 35:**
- Потрібно оновити Android Gradle Plugin до 8.2+
- Можливо, потрібна Java 17 замість Java 21
- Можуть бути проблеми з деякими плагінами

---

## 📝 Рекомендації:

### **Для стабільності:**
```gradle
compileSdk = 34  // Стабільний варіант
```

### **Для нових функцій:**
```gradle
compileSdk = 35  // Якщо потрібні API Android 15
```

### **Для максимальної сумісності:**
```gradle
compileSdk = 33  // Найбільш стабільний
minSdk = 21      // Підтримка старих пристроїв
targetSdk = 34   // Оптимізація для нових
```

---

## 🔗 Додаткова інформація:

- [Android API Levels](https://developer.android.com/guide/topics/manifest/uses-sdk-element#ApiLevels)
- [Android SDK Platform Release Notes](https://developer.android.com/about/versions/15)
- [Gradle Configuration](https://developer.android.com/build)

---

**Висновок:** `compileSdk` визначає, які API доступні під час компіляції, але не обмежує мінімальну версію Android для запуску додатку.
