# 🐳 Швидка збірка через Docker

## ✅ Виправлені проблеми:

1. **Dockerfile** — видалено неправильний синтаксис з COPY
2. **docker-compose.yml** — видалено застарілий `version`
3. **build-docker.ps1** — виправлено кодування (видалено емоджі)

---

## 🚀 Швидкий старт:

```powershell
docker-compose up --build
```

Або через скрипт:

```powershell
.\build-docker.ps1
```

---

## 📦 Результат:

APK буде в: `build\outputs\app-release.apk`

---

## ⚠️ Якщо assets не існує:

Dockerfile автоматично створює порожню директорію `assets/`, тому помилок не буде.

---

**Готово до збірки!** ✅
