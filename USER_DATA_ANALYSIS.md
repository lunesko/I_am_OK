# 📊 Аналіз даних користувача в додатку "Я ОК"

**Дата оцінки:** Січень 2026  
**Версія:** 1.0

---

## 🔍 Огляд

Додаток "Я ОК" збирає **мінімальний набір даних**, необхідний для функціонування. **Геолокація НЕ використовується**, що є ключовою особливістю безпеки.

---

## 📦 Дані користувача (UserModel)

### Зберігається в Firestore: `users/{userId}`

```dart
class UserModel {
  final String id;              // Firebase Auth UID
  final String name;             // Ім'я (з Дія ID / BankID)
  final String email;            // Email (для відновлення)
  final String? phone;           // Телефон (опціонально)
  final DateTime createdAt;      // Дата реєстрації
  final List<String> contactIds; // ID контактів користувача
}
```

### Детальний опис:

| Поле | Тип | Обов'язкове | Призначення | Приватність |
|------|-----|-------------|-------------|-------------|
| `id` | String | ✅ | Firebase Auth UID | Приватне |
| `name` | String | ✅ | Ім'я користувача | Доступне контактам |
| `email` | String | ✅ | Email для входу | Приватне |
| `phone` | String? | ❌ | Номер телефону | Приватне |
| `createdAt` | DateTime | ✅ | Дата реєстрації | Приватне |
| `contactIds` | List<String> | ✅ | Список ID контактів | Приватне |

### Додаткові поля (не в моделі, але використовуються):

| Поле | Тип | Призначення | Де зберігається |
|------|-----|-------------|-----------------|
| `pushToken` | String? | FCM токен для push-сповіщень | Firestore `users/{userId}` |
| `avatar` | String? | URL аватара (майбутнє) | Firestore `users/{userId}` |

---

## 📝 Дані чек-іну (CheckinModel)

### Зберігається в Firestore: `checkins/{checkinId}`

```dart
class CheckinModel {
  final String id;                    // Унікальний ID чек-іну
  final String userId;                // ID користувача, який відправив
  final String status;                // 'ok', 'busy', 'later', 'hug'
  final DateTime timestamp;           // Час відправки
  final bool synced;                  // Синхронізовано з сервером
  final List<String> recipientIds;   // ID отримувачів
  final Map<String, bool> readBy;    // userId: hasRead
}
```

### Детальний опис:

| Поле | Тип | Обов'язкове | Призначення | Приватність |
|------|-----|-------------|-------------|-------------|
| `id` | String | ✅ | Унікальний ID | Приватне |
| `userId` | String | ✅ | Хто відправив | Приватне |
| `status` | String | ✅ | Статус ('ok', 'busy', 'later', 'hug') | Доступне отримувачам |
| `timestamp` | DateTime | ✅ | Час відправки | Доступне отримувачам |
| `synced` | bool | ✅ | Синхронізовано | Приватне |
| `recipientIds` | List<String> | ✅ | Список отримувачів | Приватне |
| `readBy` | Map<String, bool> | ✅ | Статус прочитання | Приватне |

### Статуси чек-іну:

- `'ok'` — 💚 Я ОК
- `'busy'` — 💛 Все нормально, зайнятий
- `'later'` — 💙 Зателефоную пізніше
- `'hug'` — 🤍 Обійми

---

## 👥 Дані контакту (ContactModel)

### Зберігається локально (Hive) та синхронізується з Firestore

```dart
class ContactModel {
  final String id;              // ID контакту (Firebase UID)
  final String name;            // Ім'я контакту
  final String? avatar;         // URL аватара (майбутнє)
  final String? pushToken;      // FCM токен (для push)
  final DateTime? lastCheckin; // Останній чек-ін
  final bool isActive;          // Активний контакт
}
```

### Детальний опис:

| Поле | Тип | Обов'язкове | Призначення | Приватність |
|------|-----|-------------|-------------|-------------|
| `id` | String | ✅ | Firebase UID контакту | Приватне |
| `name` | String | ✅ | Ім'я контакту | Приватне |
| `avatar` | String? | ❌ | URL аватара | Приватне |
| `pushToken` | String? | ❌ | FCM токен (не передається) | Приватне |
| `lastCheckin` | DateTime? | ❌ | Останній чек-ін | Приватне |
| `isActive` | bool | ✅ | Активний контакт | Приватне |

---

## 🔄 Що передається через Push-сповіщення

### При створенні чек-іну:

```javascript
{
  notification: {
    title: 'Я ОК',
    body: 'Ім\'я: 💚 Я ОК',  // Ім'я відправника + статус
  },
  data: {
    type: 'checkin',
    checkinId: '...',         // ID чек-іну
    userId: '...',            // ID відправника
    status: 'ok',             // Статус
    timestamp: '...',         // ISO timestamp
  }
}
```

### При попередженні про відсутність зв'язку:

```javascript
{
  notification: {
    title: '⚠️ Попередження',
    body: 'Ім\'я не виходив на зв\'язок 3 дні',
  },
  data: {
    type: 'missing_checkin',
    userId: '...',            // ID користувача
    daysAgo: '3',             // Днів без зв'язку
  }
}
```

---

## 💾 Локальне сховище (Hive)

### Зберігається офлайн:

1. **Чек-іни** (`checkins` box):
   - Всі чек-іни користувача
   - Синхронізуються при з'явленні інтернету

2. **Контакти** (`contacts` box):
   - Список контактів
   - Кешовані дані для швидкого доступу

3. **Налаштування** (`SharedPreferences`):
   - Час нагадувань
   - Тихий режим
   - Період попередження

---

## 🔒 Правила безпеки (Firestore Rules)

### Users Collection:

```javascript
// Користувач може читати/писати тільки свій профіль
allow read, write: if request.auth.uid == userId;

// Контакти можуть читати базову інформацію (ім'я, аватар)
allow read: if request.auth.uid in resource.data.contactIds;
```

### Checkins Collection:

```javascript
// Створення: тільки власник
allow create: if request.resource.data.userId == request.auth.uid;

// Читання: власник або отримувачі
allow read: if request.auth.uid == resource.data.userId || 
              request.auth.uid in resource.data.recipientIds;

// Оновлення: тільки для позначення прочитання
allow update: if request.auth.uid in resource.data.recipientIds &&
                only 'readBy' field changed;
```

---

## 🚫 Що НЕ збирається

### ❌ Геолокація
- **Не використовується взагалі**
- Немає доступу до GPS
- Немає координат у даних

### ❌ Метадані пристрою
- IP-адреса (тільки через Firebase, не зберігається)
- MAC-адреса
- IMEI
- Інші унікальні ідентифікатори

### ❌ Аналітика поведінки
- Час використання додатку
- Частота відкриття
- Шляхи навігації
- A/B тестування

### ❌ Третя особа
- Дані не передаються рекламним мережам
- Дані не продаються
- Дані не використовуються для маркетингу

---

## 📤 Що передається між пристроями

### При відправці чек-іну:

1. **Відправник → Firestore:**
   ```json
   {
     "id": "checkin_123",
     "userId": "user_abc",
     "status": "ok",
     "timestamp": "2026-01-15T10:30:00Z",
     "recipientIds": ["user_xyz", "user_def"],
     "readBy": {}
   }
   ```

2. **Firestore → Cloud Function:**
   - Тригериться автоматично при створенні чек-іну
   - Отримує дані чек-іну
   - Отримує дані відправника (ім'я)
   - Отримує push-токени отримувачів

3. **Cloud Function → FCM:**
   - Push-сповіщення з ім'ям відправника та статусом
   - Метадані (checkinId, userId, status, timestamp)

4. **FCM → Отримувачі:**
   - Push-сповіщення на пристрої
   - Дані для обробки в додатку

---

## 🔐 Шифрування та безпека

### В транзиті:
- ✅ HTTPS для всіх запитів
- ✅ TLS для Firebase
- ✅ Шифрування FCM

### На сервері:
- ✅ Firestore автоматично шифрує дані
- ✅ Правила безпеки захищають доступ
- ✅ Push-токени зберігаються окремо

### Локально:
- ✅ Hive шифрує дані (опціонально)
- ✅ SharedPreferences захищені системою

---

## 📊 Статистика даних

### Розмір даних на користувача:

| Тип даних | Розмір | Період зберігання |
|-----------|--------|-------------------|
| User Profile | ~500 bytes | Без обмежень |
| Checkin | ~200 bytes | 30 днів |
| Contact | ~300 bytes | Без обмежень |
| Push Token | ~200 bytes | До видалення |

### Приклад обсягу даних:

- **1 користувач з 5 контактами:** ~2 KB
- **100 чек-інів:** ~20 KB
- **Загалом:** ~22 KB на користувача

---

## 🎯 Відповідність GDPR та українському законодавству

### ✅ Права користувача:

1. **Право на доступ:**
   - Користувач може переглянути всі свої дані
   - Експорт даних (майбутнє)

2. **Право на видалення:**
   - Користувач може видалити акаунт
   - Всі дані видаляються з Firestore
   - Локальні дані видаляються з пристрою

3. **Право на виправлення:**
   - Користувач може оновити ім'я, email
   - Контакти можна редагувати

4. **Право на обмеження обробки:**
   - Користувач може вимкнути push-сповіщення
   - Користувач може видалити контакти

---

## 📋 Чек-лист безпеки

### ✅ Реалізовано:

- [x] Мінімальний набір даних
- [x] Немає геолокації
- [x] Firestore Rules захищають дані
- [x] HTTPS для всіх запитів
- [x] Push-токени зберігаються окремо
- [x] Офлайн-режим з локальним сховищем

### 🚧 Потрібно додати:

- [ ] Експорт даних користувача
- [ ] Автоматичне видалення старих чек-інів (>30 днів)
- [ ] Шифрування локальних даних (Hive)
- [ ] Аудит доступу до даних
- [ ] Логування змін даних

---

## 💡 Рекомендації

### Для покращення безпеки:

1. **Додати шифрування локальних даних:**
   ```dart
   // Hive з шифруванням
   final encryptionKey = Hive.generateSecureKey();
   ```

2. **Автоматичне видалення старих даних:**
   ```javascript
   // Cloud Function для очищення
   exports.cleanupOldCheckins = functions.pubsub
     .schedule('every 24 hours')
     .onRun(async (context) => {
       // Видалити чек-іни старше 30 днів
     });
   ```

3. **Експорт даних користувача:**
   ```dart
   // Функція експорту
   Future<String> exportUserData() async {
     // Експорт у JSON
   }
   ```

---

## 📞 Контакти

- **Email підтримки:** support@poruch.app
- **Email з питань конфіденційності:** privacy@poruch.app

---

**Останнє оновлення:** Січень 2026  
**Версія документа:** 1.0

---

**Зроблено в Україні 🇺🇦**
