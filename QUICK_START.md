# 🚀 Швидкий старт для "Я ОК"

**Час виконання:** 1-2 години  
**Складність:** Середня

---

## ✅ Що вже готово

- ✅ Моделі даних (`lib/models/`)
- ✅ Сервіси (`lib/services/`)
- ✅ `main.dart` з інтеграцією
- ✅ Екрани (`lib/screens/`)
- ✅ Cloud Functions код
- ✅ Firestore Rules та Indexes

---

## 📋 Крок 1: Налаштувати Firebase (30 хв)

### 1.1. Встановити FlutterFire CLI

```bash
dart pub global activate flutterfire_cli
```

### 1.2. Налаштувати Firebase

```bash
flutterfire configure
```

**Що робити:**
1. Вибрати Firebase проект (або створити новий)
2. Вибрати платформи (Android, iOS)
3. Файл `lib/firebase_options.dart` створиться автоматично

### 1.3. Оновити main.dart

Розкоментувати в `lib/main.dart`:

```dart
import 'firebase_options.dart';

// В main():
await Firebase.initializeApp(
  options: DefaultFirebaseOptions.currentPlatform,
);
```

**Детальні інструкції:** `FIREBASE_SETUP_GUIDE.md`

---

## 📋 Крок 2: Налаштувати Firebase Console (15 хв)

### 2.1. Authentication

1. Firebase Console → **Authentication** → **Get started**
2. Увімкніть **Email/Password**

### 2.2. Firestore Database

1. Firebase Console → **Firestore Database** → **Create database**
2. Оберіть **Start in test mode**
3. Регіон: **europe-west**

### 2.3. Застосувати Rules та Indexes

```bash
firebase deploy --only firestore:rules,firestore:indexes
```

### 2.4. Cloud Messaging

1. Firebase Console → **Cloud Messaging** → **Get started**

---

## 📋 Крок 3: Розгорнути Cloud Functions (15 хв)

```bash
cd functions
npm install
firebase deploy --only functions
```

**Перевірити:**
```bash
firebase functions:list
```

---

## 📋 Крок 4: Організувати асети (15 хв)

### 4.1. Дочекатися завантаження

Перевірити, що всі файли завантажені (без `.crdownload`)

### 4.2. Запустити скрипт

```powershell
cd "M:\I am OK\logo"
powershell -ExecutionPolicy Bypass -File .\organize-files-final.ps1 -Force
```

**Детальні інструкції:** `ASSETS_ORGANIZATION_GUIDE.md`

---

## 📋 Крок 5: Запустити додаток (5 хв)

### 5.1. Встановити залежності

```bash
flutter pub get
```

### 5.2. Запустити на емуляторі

```bash
flutter run
```

---

## ✅ Чек-лист готовності

### Firebase:
- [ ] `firebase_options.dart` створено
- [ ] Authentication увімкнено
- [ ] Firestore створено
- [ ] Rules та Indexes застосовано
- [ ] Cloud Functions розгорнуто

### Асети:
- [ ] Всі файли завантажені
- [ ] Скрипт організації запущено
- [ ] Структура `assets/` створена
- [ ] `pubspec.yaml` оновлено

### Додаток:
- [ ] `flutter pub get` виконано
- [ ] Додаток компілюється
- [ ] Емулятор запущено
- [ ] Додаток працює

---

## 🐛 Типові проблеми

### Помилка: "Firebase not initialized"

**Рішення:**
1. Перевірити, що `flutterfire configure` виконано
2. Розкоментувати Firebase ініціалізацію в `main.dart`
3. Перезапустити додаток

### Помилка: "Assets not found"

**Рішення:**
1. Перевірити, що скрипт організації виконано
2. Перевірити `pubspec.yaml`
3. Виконати `flutter clean && flutter pub get`

### Помилка: "Firestore permission denied"

**Рішення:**
```bash
firebase deploy --only firestore:rules
```

---

## 📚 Детальні гайди

- **Firebase:** `FIREBASE_SETUP_GUIDE.md`
- **Асети:** `ASSETS_ORGANIZATION_GUIDE.md`
- **Розгортання:** `DEPLOYMENT_GUIDE.md`
- **Тестування:** `TESTING_GUIDE.md`

---

## 🎯 Наступні кроки

1. **Створити Family Screen** — для управління контактами
2. **Додати Success Screen** — після відправки чек-іну
3. **Інтегрувати OAuth** — для реального входу через Дія ID
4. **Тестування** — на реальних пристроях

---

**Готово!** 🎉 Додаток готовий до тестування.

---

**Зроблено в Україні 🇺🇦**
